## 2026-05-02 - Accessibility and Empty State Enhancements
**Learning:** Interactive buttons on saturated backgrounds (like `bg-emerald-600`) must explicitly set `text-white` to ensure proper contrast ratio and prevent unreadable text caused by inherited colors from parent themes.
**Action:** Always verify text contrast on primary action buttons and apply explicit text color classes.
